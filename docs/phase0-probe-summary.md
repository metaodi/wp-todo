# Phase 0 probe — answer sheet

User-Agent sent: `wp-todo-phase0-probe/0.1 (https://github.com/metaodi/wp-todo) python-urllib/3.12`

Generated by `scripts/phase0_probe.py`. Raw evidence for every line is in
`docs/phase0-probe-output.json` (workflow artifact `phase0-probe-raw`).

## P10 — the research stage's endpoints

Everything here was written from documentation without egress. These rows
say whether `src/wp_todo/enrich.py` is right.

### `prop=pageprops&ppprop=wikibase_item`

| title | item id |
| --- | --- |
| `Thalwil` | `Q68959` |
| `Adliswil` | `Q68210` |

### `prop=langlinks` — is `lllang` multi-valued?

| variant | result | langs returned |
| --- | --- | --- |
| `langlinks_piped` | HTTP 200 · 0.159s | 0: — |
| `langlinks_single` | HTTP 200 · 0.19s | 1: en |
| `langlinks_unfiltered` | HTTP 200 · 0.178s | 42: an, ar, azb, be, ca, ceb, cs, cv, en, eo, es, eu |

- `lllang` declared multi-valued: **False**
- verdict: **the piped form is wrong** (piped 0 vs single 1)

### Wikibase REST statements (`Q68959`)

| endpoint | result | top-level keys |
| --- | --- | --- |
| `statements_v1` | HTTP 200 · 0.278s | `P17`, `P31`, `P373`, `P242`, `P18`, `P281`, `P47`, `P473` |
| `statements_v0` | HTTP 404 · 0.142s | `errorKey`, `messageTranslations`, `httpCode`, `httpReason` |
| `statements_filtered` | HTTP 200 · 0.187s | `P1082` |
| `item_v1` | HTTP 200 · 0.333s | `type`, `labels`, `descriptions`, `aliases`, `statements`, `sitelinks`, `id` |

`enrich.py` expects each statement to carry `rank`, `value.type == "value"`,
`value.content`, and a `qualifiers` list of `{property: {id}, value: {...}}`.

- P1082 statement keys: `id`, `rank`, `qualifiers`, `references`, `property`, `value`
- `rank` present: **True** (`normal`)
- `value.type`: `value`
- `value.content` keys: `amount`, `unit`
- qualifier property ids: `P585`
- P585 (point in time) among them: **True**

**Shape matches what enrich.py parses: YES**

---

Probes that failed outright (transport errors):

- none

